لقد قمت بتعديل ملف سير العمل (GitHub Actions Workflow) كما طلبت:

1. تم تغيير الـ tag الافتراضي ليصبح `latest` بدلاً من إنشاء tag جديد لكل عملية بناء، وتمت إزالة خيار `prerelease`.
2. تم تعديل تسمية الإصدار وتسمية ملف التطبيق (APK) المرفوع ليصبح `Wathakker V1.1 Preview (Build X).apk` (حيث X هو رقم البناء).
3. تمت إضافة خطوة لإعادة تسمية الـ APK قبل رفعه، لضمان أن يحمل الملف نفس الاسم المطلوب مع حرف V قبل رقم الإصدار.

يرجى دفع التغييرات (Push) وتشغيل البناء للتحقق من النتيجة.
