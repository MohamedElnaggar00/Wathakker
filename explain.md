مرحباً! لقد تأكدت من الإعدادات، وإليك ما سيحدث بالتفصيل:

بالنسبة للإصدار ورقم التحديث (`versionCode` و `versionName`) و `applicationId`: لقد قمنا بالفعل بضبطهم بشكل صحيح. رقم الإصدار أصبح `2` (أعلى من السابق)، والـ `applicationId` هو `com.wathakker.app` وهو ثابت.

**ولكن هناك مشكلة تقنية متعلقة بـ "توقيع التطبيق" (App Signature):**
لكي يسمح نظام أندرويد بتحديث التطبيق دون الحاجة لحذفه (بدون تعارض)، **يجب** أن يكون التطبيق الجديد موقعاً بنفس الـ Keystore الذي تم توقيع الإصدار القديم به.
حالياً، سكربت GitHub Actions الخاص بك (`build-apk.yml`) يقوم ببناء نسخة `Debug` ويقوم بتوليد مفتاح جديد عشوائي في كل مرة يعمل فيها:
`keytool -genkey -v -keystore debug.keystore ...`
بما أن المفتاح يتغير في كل مرة بناء، فإن أندرويد سيرفض التحديث وسيطلب منك حذف التطبيق القديم أولاً. (مفتاح الإصدار القديم فُقد لأنه لم يُحفظ).

**كيف نصلح ذلك بشكل احترافي للمستقبل ونرفع نسخة Release؟**
لحل هذه المشكلة للأبد، يجب أن ننتقل إلى بناء نسخة `Release` بدلاً من `Debug`، ويجب أن يكون لدينا مفتاح `Keystore` ثابت محفوظ بأمان في `GitHub Secrets`.

**الخطوات المطلوبة منك (إذا كنت تريد التحديثات التلقائية بدون مسح التطبيق مستقبلاً):**
1. قم بإنشاء ملف `keystore` (مفتاح توقيع) على جهازك.
2. قم بتحويله إلى نص (Base64) وأضفه في إعدادات GitHub Secrets (تحت اسم `KEYSTORE_BASE64`).
3. أضف كلمات السر الخاصة بالمفتاح كـ Secrets (مثلاً `KEYSTORE_PASSWORD`, `KEY_ALIAS`, `KEY_PASSWORD`).

في الوقت الحالي، هل تريدني أن أقوم بتعديل ملف `build-apk.yml` ليقوم ببناء `assembleRelease` واستخدام الـ Secrets المذكورة أعلاه ليصبح جاهزاً عندما تقوم بوضع المفتاح في GitHub؟ (علماً بأنك ستحتاج لحذف التطبيق القديم هذه المرة فقط من هاتفك، وبعدها ستعمل التحديثات المستقبلية بسلاسة).

وبالنسبة لرفع الـ APK، لقد تأكدت من تعديل الـ workflow ليرفع الملف باسم `Wathakker 1.1 preview.apk` مع تفعيل خاصية `overwrite: false`، مما يعني أنه سيتم إضافته بجانب الإصدار القديم (`WathakkerV1.0 preview.apk`) في صفحة الـ Releases على GitHub ولن يتم مسح القديم.
