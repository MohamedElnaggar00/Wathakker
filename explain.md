لقد قمت بتغيير الأداة المستخدمة في GitHub Actions لرفع الـ APK إلى أداة أكثر استقراراً (`softprops/action-gh-release`)، وقمت ببرمجتها لإنشاء إصدار (Release) جديد لكل عملية بناء باستخدام رقم البناء (`github.run_number`).

هذا سيضمن عدم حدوث خطأ "الاسم موجود مسبقاً" (already exists)، حيث سيكون لكل بناء رقم فريد واسم مختلف.

يرجى دفع (Push) التغييرات أو تشغيل البناء مرة أخرى، وسيعمل هذه المرة بنجاح إن شاء الله!
